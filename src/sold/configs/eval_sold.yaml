defaults:
  - hydra: default

checkpoint_path: "/home/user/mosbach/PycharmProjects/sold/experiments/train_sold/jsonl/2024-12-06_16-16-21/logs/version_0/checkpoints/"  # Can be one checkpoint or a directory of checkpoints.
experiment: "my_experiment"
eval_episodes: 4
seed: 42

env:
  _target_: sold.envs.make_env
  suite: "mof"  # Should be "mof", "gym", or "dmcontrol".
  name: ReachRed_0to4Distractors_Dense-v1
  image_size: [ 64, 64 ]
  max_episode_steps: 50
  action_repeat: 2